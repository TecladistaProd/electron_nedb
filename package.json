{
  "name": "elnedb",
  "version": "0.0.1",
  "description": "Electron nedb integration",
  "main": "app.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . meuApp --overwrite --asar=true --platform=win32 --arch=ia32 --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"ListadeContatos\""
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "electron": "^1.7.10",
    "fs": "0.0.1-security",
    "nedb-promise": "^2.0.1",
    "node-notifier": "^5.2.1"
  },
  "devDependencies": {
    "electron-packager": "^11.1.0"
  }
}
